import { styled } from "@material-ui/core";

export const UserDocumentsContainer = styled('div')({
    display: 'flex',
    flexDirection: 'column',
    flex: 1
});

export const UserCardContainer = styled('div')({
    display: 'flex'
});

export const UserDocumentsListContainer = styled('div')({
    display: 'flex',
    flex: 1
});